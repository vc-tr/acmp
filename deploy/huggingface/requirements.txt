# Hugging Face Spaces requirements — lightweight, CPU-only (no torch/AI).
# The Streamlit demo defaults to the no-AI Ken Burns pipeline so it fits the free tier.
opencv-python-headless>=4.8,<4.10
Pillow>=10.0,<12
numpy>=1.24,<2
moviepy>=2.0
imageio-ffmpeg>=0.4
pyyaml>=6.0
PyMuPDF>=1.23
tqdm>=4.65
streamlit>=1.30,<1.40
