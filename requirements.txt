# Core
opencv-python-headless>=4.8,<4.10  # headless (no libGL); <4.10 keeps numpy<2 compatible
Pillow>=10.0,<12                    # moviepy/streamlit require Pillow <12
numpy>=1.24,<2                      # numpy 2.x breaks streamlit/moviepy/matplotlib in this stack
moviepy>=2.0
pyyaml>=6.0
PyMuPDF>=1.23
click>=8.0
tqdm>=4.65

# AI Animation (Wan VACE 1.3B)
torch>=2.1
torchvision>=0.16
diffusers>=0.30
accelerate>=0.25

# Scene Analysis
anthropic>=0.40

# Character Segmentation
rembg[cpu]>=2.0

# Depth Estimation (v1 fallback)
timm>=0.9
